{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd HerdHub && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd HerdHub && node server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
